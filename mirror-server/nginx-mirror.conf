server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    root /var/www/mirror;
    sendfile on;
    tcp_nopush on;
    location / {
        try_files $uri $uri/ =404;
    }
}
